<h4 class="article-title list-group-item-heading">{{article.title}}</h4>
<div class="meta list-group-item-text">
  <div>{{moment-format article.updatedDate 'LLL'}}</div>
  {{#if article.author}}
    <i>By {{article.author}}</i>
  {{/if}}
</div>
