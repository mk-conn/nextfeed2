import debugUtil from 'frontend/utils/debug-util';
import { module, test } from 'qunit';

module('Unit | Utility | debug util');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = debugUtil();
  assert.ok(result);
});
